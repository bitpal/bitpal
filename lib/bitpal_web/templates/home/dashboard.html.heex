Stores:

<ul>
  <%= for store <- @stores do %>
    <li><%= link(store.label, to: Routes.store_path(@socket, :show, store)) %></li>
  <% end %>
</ul>

<%= link("Server settings", to: Routes.server_settings_path(@socket, :show)) %>

TODO Server status
